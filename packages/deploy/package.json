{
    "name": "@xqoder/deploy",
    "version": "0.1.0-rc.202603230340",
    "private": true,
    "type": "module",
    "description": "XQoder Cloud Deploy - automated deployment",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "clean": "rm -rf dist",
        "test": "vitest run",
        "lint": "tsc --noEmit"
    },
    "dependencies": {
        "@xqoder/shared": "workspace:*"
    },
    "devDependencies": {
        "typescript": "^5.4.0",
        "vitest": "^2.0.0"
    }
}
