{
    "name": "@xqoder/workflow",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "XQoder Workflow Engine - task orchestration",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "clean": "rm -rf dist",
        "test": "vitest run",
        "typecheck": "tsc --noEmit",
        "lint": "tsc --noEmit"
    },
    "dependencies": {
        "@xqoder/shared": "workspace:*",
        "@xqoder/agent": "workspace:*",
        "@xqoder/runtime": "workspace:*"
    },
    "devDependencies": {
        "typescript": "^5.4.0",
        "vitest": "^2.0.0"
    }
}
